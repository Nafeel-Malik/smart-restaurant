import DarkSelect from '../ui/DarkSelect'

/** Styled select — delegates to DarkSelect for consistent dark theme + chevron. */
export default function AnimatedSelect(props) {
  return <DarkSelect {...props} />
}
